name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # ✅ Pages 설정 + base 경로 자동 제공
      - uses: actions/configure-pages@v5

      - run: npm ci

      # ✅ Vite가 base를 자동 반영하게 주입
      - name: Build
        run: npm run build
        env:
          # repo pages면 보통 /Jumpers/ 가 들어감
          BASE_URL: ${{ steps.configure-pages.outputs.base_path }}

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
